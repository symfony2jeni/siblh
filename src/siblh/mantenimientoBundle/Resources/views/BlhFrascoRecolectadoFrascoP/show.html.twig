{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlhFrascoRecolectadoFrascoP</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Volumenagregado</th>
                <td>{{ entity.volumenAgregado }}</td>
            </tr>
            <tr>
                <th>Usuario</th>
                <td>{{ entity.usuario }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('blhfrascorecolectadofrascop') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('blhfrascorecolectadofrascop_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
