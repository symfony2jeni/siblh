{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlhSubOpcionMenu</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombresubopcion</th>
                <td>{{ entity.nombreSubOpcion }}</td>
            </tr>
            <tr>
                <th>Urlsubopcion</th>
                <td>{{ entity.urlSubOpcion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('blhsubopcionmenu') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('blhsubopcionmenu_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
