{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlhRol</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombrerol</th>
                <td>{{ entity.nombreRol }}</td>
            </tr>
            <tr>
                <th>Descripcionrol</th>
                <td>{{ entity.descripcionRol }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('blhrol') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('blhrol_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
