{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlhDonacion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fechadonacion</th>
                <th>Responsabledonacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('blhdonacion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fechaDonacion %}{{ entity.fechaDonacion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.responsableDonacion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('blhdonacion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('blhdonacion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('blhdonacion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
