{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlhFrascoRecolectado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Codigofrascorecolectado</th>
                <td>{{ entity.codigoFrascoRecolectado }}</td>
            </tr>
            <tr>
                <th>Volumenrecolectado</th>
                <td>{{ entity.volumenRecolectado }}</td>
            </tr>
            <tr>
                <th>Formaextraccion</th>
                <td>{{ entity.formaExtraccion }}</td>
            </tr>
            <tr>
                <th>Onzrecolectado</th>
                <td>{{ entity.onzRecolectado }}</td>
            </tr>
            <tr>
                <th>Observacionfrascorecolectado</th>
                <td>{{ entity.observacionFrascoRecolectado }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('blhfrascorecolectado') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('blhfrascorecolectado_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
