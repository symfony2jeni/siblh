 {% extends 'siblhmantenimientoBundle::plantilla.html.twig' %} 

{% block body -%}
    <h1>BlhAnalisisMicrobiologico list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Codigoanalisismicrobiologico</th>
                <th>Coliformestotales</th>
                <th>Control</th>
                <th>Situacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('blhanalisismicrobiologico_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.codigoAnalisisMicrobiologico }}</td>
                <td>{{ entity.coliformesTotales }}</td>
                <td>{{ entity.control }}</td>
                <td>{{ entity.situacion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('blhanalisismicrobiologico_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('blhanalisismicrobiologico_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('blhanalisismicrobiologico_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
