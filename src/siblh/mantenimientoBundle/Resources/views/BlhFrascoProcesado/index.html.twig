{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlhFrascoProcesado list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Codigofrascoprocesado</th>
                <th>Volumenfrascopasteurizado</th>
                <th>Acideztotal</th>
                <th>Kcaloriastotales</th>
                <th>Observacionfrascoprocesado</th>
                <th>Volumendisponiblefp</th>
                <th>Usuario</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('blhfrascoprocesado_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.codigoFrascoProcesado }}</td>
                <td>{{ entity.volumenFrascoPasteurizado }}</td>
                <td>{{ entity.acidezTotal }}</td>
                <td>{{ entity.kcaloriasTotales }}</td>
                <td>{{ entity.observacionFrascoProcesado }}</td>
                <td>{{ entity.volumenDisponibleFp }}</td>
                <td>{{ entity.usuario }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('blhfrascoprocesado_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('blhfrascoprocesado_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('blhfrascoprocesado_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
