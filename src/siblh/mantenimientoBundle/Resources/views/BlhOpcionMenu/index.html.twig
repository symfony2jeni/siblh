{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlhOpcionMenu list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombreopcion</th>
                <th>Urlopcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('blhopcionmenu_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreOpcion }}</td>
                <td>{{ entity.urlOpcion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('blhopcionmenu_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('blhopcionmenu_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('blhopcionmenu_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
