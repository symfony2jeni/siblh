{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlhLoteAnalizado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Codigoloteanalizado</th>
                <td>{{ entity.codigoLoteAnalizado }}</td>
            </tr>
            <tr>
                <th>Fechaanalisisfisicoquimico</th>
                <td>{{ entity.fechaAnalisisFisicoQuimico|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('blhloteanalizado') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('blhloteanalizado_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
