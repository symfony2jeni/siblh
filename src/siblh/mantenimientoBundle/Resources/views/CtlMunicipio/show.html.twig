{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CtlMunicipio</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Codigocnr</th>
                <td>{{ entity.codigoCnr }}</td>
            </tr>
            <tr>
                <th>Abreviatura</th>
                <td>{{ entity.abreviatura }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ctlmunicipio') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ctlmunicipio_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
