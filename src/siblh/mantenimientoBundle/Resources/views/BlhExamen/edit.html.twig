{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlhExamen edit</h1>

    {{ form(edit_form) }}
        {{ form_widget(edit_form.usuario, {'attr':{ 'value' : user_ID }}) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('blhexamen') }}">
            Back to the list
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
