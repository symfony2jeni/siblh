{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlhFrascoProcesadoSolicitud list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Volumendespachado</th>
                <th>Usuario</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('blhfrascoprocesadosolicitud_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.volumenDespachado }}</td>
                <td>{{ entity.usuario }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('blhfrascoprocesadosolicitud_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('blhfrascoprocesadosolicitud_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('blhfrascoprocesadosolicitud_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
