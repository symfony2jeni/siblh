{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlhTemperaturaEnfriamiento</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Temperaturae</th>
                <td>{{ entity.temperaturaE }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('blhtemperaturaenfriamiento') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('blhtemperaturaenfriamiento_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
