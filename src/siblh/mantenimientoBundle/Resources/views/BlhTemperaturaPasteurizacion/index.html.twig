{% extends 'siblhmantenimientoBundle::plantilla.html.twig' %}

{#{% block javascripts %}
    {{parent()}}
    <script type="text/javascript" src="{{ asset('bundles/siblhmantenimiento/js/BlhTempPasteurizacion/index.js') }}" ></script>
{% endblock %}  #}


{% block body -%}
    <h1>BlhTemperaturaPasteurizacion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Temperaturap</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('blhtemperaturapasteurizacion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.temperaturaP }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('blhtemperaturapasteurizacion_show', { 'id': entity.id }) }}">Detalle</a>
                    </li>
                    <li>
                        <a href="{{ path('blhtemperaturapasteurizacion_edit', { 'id': entity.id }) }}">Actualizar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

   
        <ul>
        <li>
            <a href="{{ path('blhtemppasteurizacion') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}